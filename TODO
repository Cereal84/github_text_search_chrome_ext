TODO list

    [x] Use local storage in order to save matchs and search text
    [x] User can choose the background color to highlight the matches
    [x] Add auto focus on match
    [x] Add shortcut for Linux
    [x] Add button to move around the matches
    [x] Add shortcut for MAC
    [x] When go back to the repo page redo last search
    [x] Clean data when search empty string

    - add timestamp in order to clean old data ( old == 1h)
    - add settings to choose the position of panel with button
    - add checkbox to have panel to move around the matches
    - add button to clear data from local storage for current search
    - add button to clear all data from local storage
